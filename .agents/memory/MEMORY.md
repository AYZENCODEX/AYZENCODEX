- [AYZEN Auth Pattern](ayzen-auth.md) — Mock auth uses SHA-256+static salt + base64 JSON tokens; OTP registration added (in-memory Map, 10min expiry); magic-link uses Resend OTP not Supabase.
- [Orval Body Schema Naming](orval-body-naming.md) — Body schemas must use entity-shaped names (NoteInput, not CreateNoteBody) or TS2308 collisions break the build after codegen.
- [Express 5 Wildcard Routes](express5-routes.md) — Use `/{*splat}` not `*` for catch-all routes; `req.params.id` is `string|string[]`, always parse with Array.isArray guard.
- [AYZEN Projects API Shape](ayzen-api-shapes.md) — /api/projects returns {projects, total, page, limit} NOT array; /api/tasks returns array. Frontend must handle both.
- [AYZEN Password Hash](ayzen-auth.md) — Hash uses "ayzen_salt" constant (not env var). change-password endpoint verified working after server restart.
- [AYZEN DB Pool Config](ayzen-db-pool.md) — Supabase transaction pooler (port 6543) breaks Drizzle prepared statements; buildPoolConfig in lib/db/src/index.ts auto-switches to session pooler (5432) + ssl rejectUnauthorized:false.
- [AYZEN DB Schema Migration](ayzen-db-schema.md) — When DATABASE_URL points to a different Supabase project, tables may have UUID ids or wrong columns; must drop+recreate projects/tasks/users/user_projects with integer serial ids.
- [AYZEN Startup Migrations](ayzen-startup-migrations.md) — Runtime ALTER TABLE migrations run 2s after server start via waitForDbThenMigrate in api-server/src/index.ts; logBus output goes to SSE stream NOT stdout, so they won't appear in workflow log files. Verify by calling API endpoints, not by reading logs.
- [AYZEN XP+AZN System](ayzen-xp-azn.md) — tasks.xpAmount × projects.xpPrice = AZN awarded on task approval; awardXpAsAzn helper in tasks.ts route auto-triggers on status→approved.
- [AYZEN Error Library](ayzen-error-library.md) — AppError class + ERROR_LIBRARY in lib/errors.ts; global handler in middlewares/error-handler.ts; GET /api/functions lists 120 endpoints; GET /api/functions/errors lists all codes+solutions.
- [AYZEN Auth OTP Signup](ayzen-auth-otp-signup.md) — login.tsx signup tab is 2-step: form → OTP verify. Both login.tsx and register.tsx call /api/auth/send-otp then pass emailOtp to /api/auth/register. useEffect handles countdown (not useState).
- [AYZEN Production Deployment](ayzen-deployment.md) — Express serves built frontend (artifacts/ayzen/dist/public/) when NODE_ENV=production; deploy on single port 8080; build cmd: build API + build frontend. Deployment target: autoscale.
- [AYZEN Task Submit Modal](ayzen-task-submit.md) — SubmitModal Cost/ROI tab has COST_CATEGORIES (Gas Fee, Account Create Fee, Swap Fee, Bridge Fee, Net Fee, Manual) and PROFIT_CATEGORIES (Refer, Trade Volume, Mystery Box, FCFS, Random, TGE, Manual) as pill selectors. costCategory/profitCategory sent in submit payload.
- [AYZEN Activity Log](ayzen-activity.md) — user_activity table; GET /api/history (user), GET /admin/history (admin, paginated: page+limit+action filter returns {entries,total,page,limit}). Frontend: /history (user), /admin/activity (admin).
- [AYZEN Auth OTP Signin](ayzen-auth-otp-signup.md) — Signin is now 2-step: credentials → OTP. POST /auth/login verifies credentials, then /auth/send-otp sends OTP; POST /auth/verify-otp validates OTP (no session). Login.tsx uses signinStep:"form"|"otp" + tempSigninData.
- [AYZEN Vault Tabs](ayzen-vault-tabs.md) — Vault page rebuilt with 4 top horizontal tabs: Entity | Wallet | Local | 2FA. Entity tab = vault entity grid; Wallet tab = lazy UserWallets; Local tab = LocalAccounts component; 2FA tab = lazy Authenticator. No internal sidebar.
- [AYZEN Admin Sidebar Groups](ayzen-admin-sidebar.md) — Admin nav split into: Platform, Operators, Protocols, Tools, Community, Finance, Monitoring, Config. Activity Log is under Monitoring at /admin/activity.
- [AYZEN Wallets Table](ayzen-wallets.md) — wallets table defined in lib/db/src/schema/wallets.ts; CREATE TABLE added to startup MIGRATIONS array (not just ALTER); use-wallet-connect hook exists at artifacts/ayzen/src/hooks/use-wallet-connect.ts.
- [AYZEN LocalAccounts Platform Tab](ayzen-local-accounts-platform.md) — AccountFormDialog has 5 tabs: creds/dates/value/platform/points; Points tab (5th) tracks per-account points balance via local_account_points table; GET+POST /api/local-accounts/:id/points; DELETE /api/local-accounts/points/:pointId.
- [AYZEN API Server Workflow](ayzen-workflow.md) — "API Server" workflow must run dist directly: `cd artifacts/api-server && PORT=8000 node --enable-source-maps ./dist/index.mjs`. Build first via bash: `pnpm --filter @workspace/api-server run build`. Old pnpm dev command times out restart_workflow (build step >60s).
